name: Flask App CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Checkout the repository code
      - name: Checkout code
        uses: actions/checkout@v2

      # Set up Python
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: "3.9"

      # Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

  deploy:
    runs-on: ubuntu-latest
    needs: build
    if: success()

    steps:
      # Checkout code
      - name: Checkout code
        uses: actions/checkout@v2

      # Debug: Check if Railway CLI URL is responding correctly
      - name: Debug Railway CLI URL
        run: |
          echo "Checking if Railway CLI URL is responding correctly"
          curl -I https://railway.app/cli/install.sh  # Get response headers

      # Install Railway CLI using the recommended method from Railway docs
      - name: Install Railway CLI
        run: |
          echo "Attempting to download and install Railway CLI"
          bash <(curl -fsSL cli.new)
          if [ $? -ne 0 ]; then
            echo "Error installing Railway CLI"
            exit 1
          fi
          echo "Railway CLI installed successfully."

      # Set Railway API key for non-interactive authentication
      - name: Set Railway API Key
        run: |
          echo "Setting Railway API key for non-interactive authentication"
          railway config set RAILWAY_API_KEY ${{ secrets.RAILWAY_API_KEY }}

      # Deploy the Flask app to Railway
      - name: Deploy to Railway
        run: |
          echo "Deploying Flask app to Railway"
          railway up --service flask-app
        env:
          RAILWAY_API_KEY: ${{ secrets.RAILWAY_API_KEY }} # Provide API Key as an environment variable
